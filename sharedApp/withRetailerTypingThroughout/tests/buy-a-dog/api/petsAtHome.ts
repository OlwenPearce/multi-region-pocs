describe("petsAtHome API" => {
    //tests here use a stubbed database etc but real client types
    //and ideally real fasify setup to test the plugins get the right client

    it("should retive dog", () => {
        //make request to API
        //mock database to return a dog
        //assert response type
    })
})
